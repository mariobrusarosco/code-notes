// Project's Package JSON
const packageJSON = require('../../package.json')

const commonConfiguration = {
  appName: "Code Notes",
  version: packageJSON.vesion
}

module.exports = commonConfiguration
