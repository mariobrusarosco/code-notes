import { shallow } from 'enzyme'

import Counter from '../index'

it('renders without error', () => {
  shallow(<Counter />)
})

it('', () => {
  expect(false).toBe(true)
})

it('', () => {
  expect(false).toBe(true)
})

it('', () => {
  expect(false).toBe(true)
})

it('', () => {
  expect(false).toBe(true)
})
