const initialState = {
  description: 'Display data',
  body: null,
  mode: 'javascript',
  theme: 'darcula',
  editor: null,
  CodeMirror: null
}

const EditorReducer = (state = initialState) => {
  return state
}

export default EditorReducer
